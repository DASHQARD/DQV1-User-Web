/** Full-width on mobile with exactly 16px (px-4) horizontal inset — no .wrapper 91% gutter. */
export const VENDOR_PROFILE_WRAPPER =
  'wrapper max-md:w-full max-md:max-w-none max-md:mx-0 max-md:px-4'

/** Panel chrome on md+; flat on mobile (matches card details). */
export const VENDOR_PROFILE_PANEL =
  'max-md:border-0 max-md:p-0 max-md:shadow-none max-md:bg-transparent max-md:rounded-none rounded-xl md:rounded-2xl border border-gray-200 bg-white p-4 md:p-6 shadow-sm'

/** DashGo purchase block — single card on desktop, split on mobile. */
export const VENDOR_DASHGO_SECTION =
  'lg:rounded-2xl lg:border lg:border-gray-200 lg:bg-white lg:shadow-sm lg:overflow-hidden'

export const VENDOR_DASHGO_FORM =
  'space-y-5 md:space-y-6 max-md:border-0 max-md:p-0 max-md:shadow-none max-md:bg-transparent max-md:rounded-none lg:p-0 lg:border-0 lg:shadow-none lg:bg-transparent lg:rounded-none'
